// GENERATED BY Macro: DO NOT MODIFY BY HAND

part of 'main.dart';

mixin UserProfileData {
  static UserProfile fromJson(Map<String, dynamic> json) {
    return UserProfile(
      name: json['name'] as String,
      age: (json['age'] as num).toInt(),
    );
  }

  Map<String, dynamic> toJson() {
    final v = this as UserProfile;
    return <String, dynamic>{
      'name': v.name,
      'age': v.age,
    };
  }

  UserProfile copyWith({
    String? name,
    int? age,
  }) {
    final v = this as UserProfile;
    return UserProfile(
      name: name ?? v.name,
      age: age ?? v.age,
    );
  }

  @override
  bool operator ==(Object other) {
    final v = this as UserProfile;
    return identical(this, other) ||
        (other.runtimeType == runtimeType &&
            other is UserProfile &&
            (identical(other.name, v.name) || other.name == v.name) &&
            (identical(other.age, v.age) || other.age == v.age));
  }

  @override
  int get hashCode {
    final v = this as UserProfile;
    return Object.hash(
      runtimeType,
      v.name,
      v.age,
    );
  }

  @override
  String toString() {
    final v = this as UserProfile;
    return 'UserProfile{name: ${v.name}, age: ${v.age}}';
  }
}

mixin AnimalData {
  static Animal fromJson(Map<String, dynamic> json) {
    final type = json['type'];
    return switch (type) {
      'Cat' => CatData.fromJson(json),
      'bow' => CowData.fromJson(json),
      'its_dog' => DogData.fromJson(json),
      _ => throw InvalidDiscriminatorException(
        'Unrecognized discriminator value "$type" for Animal. No default subtype is defined.',
      ),
    };
  }

  Map<String, dynamic> toJsonBy() {
    return switch (this) {
      Cat v => v.toJson(),
      Cow v => v.toJson(),
      Dog v => v.toJson(),
      _ => throw InvalidDiscriminatorException('Unrecognized discriminator value "$runtimeType" for Animal.'),
    };
  }

  Res map<Res>({
    required Res Function(Cat value) cat,
    required Res Function(Cow value) cow,
    required Res Function(Dog value) dog,
    Res Function(Animal value)? fallback,
  }) {
    return switch (this) {
      Cat v => cat(v),
      Cow v => cow(v),
      Dog v => dog(v),
      _ when fallback != null => fallback(this as Animal),
      _ => throw InvalidDiscriminatorException('Unrecognized discriminator value "$runtimeType" for Animal.'),
    };
  }

  Res? mapOrNull<Res>({
    Res? Function(Cat value)? cat,
    Res? Function(Cow value)? cow,
    Res? Function(Dog value)? dog,
  }) {
    return switch (this) {
      Cat v => cat?.call(v),
      Cow v => cow?.call(v),
      Dog v => dog?.call(v),
      _ => null,
    };
  }

  Animal copyWithBy({
    Cat Function(Cat value)? cat,
    Cow Function(Cow value)? cow,
    Dog Function(Dog value)? dog,
  }) {
    return switch (this) {
      Cat v => cat != null ? cat(v) : v.copyWith(),
      Cow v => cow != null ? cow(v) : v.copyWith(),
      Dog v => dog != null ? dog(v) : v.copyWith(),
      _ => throw InvalidDiscriminatorException('Unrecognized discriminator value "$runtimeType" for Animal.'),
    };
  }

  @pragma('vm:prefer-inline')
  Cat asCat() {
    return this as Cat;
  }

  @pragma('vm:prefer-inline')
  Cow asCow() {
    return this as Cow;
  }

  @pragma('vm:prefer-inline')
  Dog asDog() {
    return this as Dog;
  }
}

mixin CatData {
  static Cat fromJson(Map<String, dynamic> json) {
    return Cat(
      nickName: json['nickName'] as String,
      name: json['name'] as String,
    );
  }

  Map<String, dynamic> toJson() {
    final v = this as Cat;
    return <String, dynamic>{
      'nickName': v.nickName,
      'name': v.name,
      'type': 'Cat',
    };
  }

  Cat copyWith({
    String? nickName,
    String? name,
  }) {
    final v = this as Cat;
    return Cat(
      nickName: nickName ?? v.nickName,
      name: name ?? v.name,
    );
  }

  @override
  bool operator ==(Object other) {
    final v = this as Cat;
    return identical(this, other) ||
        (other.runtimeType == runtimeType &&
            other is Cat &&
            (identical(other.nickName, v.nickName) || other.nickName == v.nickName) &&
            (identical(other.name, v.name) || other.name == v.name));
  }

  @override
  int get hashCode {
    final v = this as Cat;
    return Object.hash(
      runtimeType,
      v.nickName,
      v.name,
    );
  }

  @override
  String toString() {
    final v = this as Cat;
    return 'Cat{nickName: ${v.nickName}, name: ${v.name}}';
  }
}

mixin DogData {
  static Dog fromJson(Map<String, dynamic> json) {
    return Dog(
      big: json['big'] as bool,
      name: json['name'] as String,
    );
  }

  Map<String, dynamic> toJson() {
    final v = this as Dog;
    return <String, dynamic>{
      'big': v.big,
      'name': v.name,
      'type': 'its_dog',
    };
  }

  Dog copyWith({
    bool? big,
    String? name,
  }) {
    final v = this as Dog;
    return Dog(
      big: big ?? v.big,
      name: name ?? v.name,
    );
  }

  @override
  bool operator ==(Object other) {
    final v = this as Dog;
    return identical(this, other) ||
        (other.runtimeType == runtimeType &&
            other is Dog &&
            (identical(other.big, v.big) || other.big == v.big) &&
            (identical(other.name, v.name) || other.name == v.name));
  }

  @override
  int get hashCode {
    final v = this as Dog;
    return Object.hash(
      runtimeType,
      v.big,
      v.name,
    );
  }

  @override
  String toString() {
    final v = this as Dog;
    return 'Dog{big: ${v.big}, name: ${v.name}}';
  }
}

mixin CowData {
  static Cow fromJson(Map<String, dynamic> json) {
    return Cow(
      big: json['big'] as bool,
      name: json['name'] as String,
    );
  }

  Map<String, dynamic> toJson() {
    final v = this as Cow;
    return <String, dynamic>{
      'big': v.big,
      'name': v.name,
      'type': 'bow',
    };
  }

  Cow copyWith({
    bool? big,
    String? name,
  }) {
    final v = this as Cow;
    return Cow(
      big: big ?? v.big,
      name: name ?? v.name,
    );
  }

  @override
  bool operator ==(Object other) {
    final v = this as Cow;
    return identical(this, other) ||
        (other.runtimeType == runtimeType &&
            other is Cow &&
            (identical(other.big, v.big) || other.big == v.big) &&
            (identical(other.name, v.name) || other.name == v.name));
  }

  @override
  int get hashCode {
    final v = this as Cow;
    return Object.hash(
      runtimeType,
      v.big,
      v.name,
    );
  }

  @override
  String toString() {
    final v = this as Cow;
    return 'Cow{big: ${v.big}, name: ${v.name}}';
  }
}
